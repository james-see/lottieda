{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Allow the main editor window to use core APIs and native file dialogs.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
